
$(function () {
  // Admin Panel settings

  //****************************
  /* This is for the mini-sidebar if width is less then 1170*/
  //****************************
  var setsidebartype = function () {
    var width =
      window.innerWidth > 0 ? window.innerWidth : this.screen.width;
    if (width < 1199) {
      $("#main-wrapper").attr("data-sidebartype", "mini-sidebar");
      $("#main-wrapper").addClass("mini-sidebar");
    } else {
      $("#main-wrapper").attr("data-sidebartype", "full");
      $("#main-wrapper").removeClass("mini-sidebar");
    }
  };
  $(window).ready(setsidebartype);
  $(window).on("resize", setsidebartype);
  //****************************
  /* This is for sidebartoggler*/
  //****************************
  $(".sidebartoggler").on("click", function () {
    $("#main-wrapper").toggleClass("mini-sidebar");
    if ($("#main-wrapper").hasClass("mini-sidebar")) {
      $(".sidebartoggler").prop("checked", !0);
      $("#main-wrapper").attr("data-sidebartype", "mini-sidebar");
    } else {
      $(".sidebartoggler").prop("checked", !1);
      $("#main-wrapper").attr("data-sidebartype", "full");
    }
  });
  $(".sidebartoggler").on("click", function () {
    $("#main-wrapper").toggleClass("show-sidebar");
  });
})

// Modal công việc
var modalJob = document.getElementById("myModal-job");
var btnJob = document.getElementById("createBtn-job");
var spanJob = modalJob.getElementsByClassName("close")[0];

btnJob.onclick = function() {
    modalJob.style.display = "block";
}

spanJob.onclick = function() {
    modalJob.style.display = "none";
}

// Đóng modal công việc khi nhấp chuột bên ngoài nó
window.onclick = function(event) {
    if (event.target == modalJob) {
        modalJob.style.display = "none";
    }
}

// Modal nhân viên
var modalStaff = document.getElementById("myModal-staff");
var btnStaff = document.getElementById("createBtn-staff");
var spanStaff = modalStaff.getElementsByClassName("close")[0];

btnStaff.onclick = function() {
    modalStaff.style.display = "block";
}

spanStaff.onclick = function() {
    modalStaff.style.display = "none";
}

